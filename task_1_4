;; The first three lines of this file were inserted by DrScheme. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname task_1_4) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ())))
(define (a-plus-abs-b a b)
  (
   (if (> b 0) + -) 
   a b
   )
  ) ;b > 0 - определяется действие '+' рассчитывается a + b; b < 0 - действие '-', вычисляется a - b